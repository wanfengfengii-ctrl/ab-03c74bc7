{
  "name": "metal-desalination-console",
  "version": "1.0.0",
  "private": true,
  "description": "金属文物脱盐换液本地控制台（事件溯源 + 修订号乐观并发）",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "smoke": "node --experimental-strip-types scripts/smoke.ts",
    "verify": "vitest run && tsc --noEmit && vite build && node --experimental-strip-types scripts/smoke.ts"
  },
  "devDependencies": {
    "@types/node": "^22.20.4",
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vitest": "^2.1.5"
  }
}
